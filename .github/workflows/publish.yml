name: Publish
on: [push]
jobs:
  Publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          repository: jensjepsen/growing-neural-cellular-automata
      - run: |
          cd frontend
          npm run build